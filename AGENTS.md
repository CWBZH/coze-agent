# AGENTS.md

## 必读上下文

在执行任何任务前，必须先阅读以下文件：

1. `PROJECT_CONTEXT.md`
2. `ARCHITECTURE.md`
3. `TASKS.md`
4. `CODEX_RULES.md`
5. `DECISIONS.md`

如果这些文件不存在，先提醒用户，不要直接开始大规模修改。

## 项目定位

这是一个面向拼多多商家的 AI 客服助手项目。

当前阶段目标：

从 Windows 本地可运行原型，逐步升级为可交付的 Linux 私有化部署版本。

当前重点不是 SaaS，不是多租户，不是计费系统，而是：

1. 稳定运行
2. 可部署
3. 可诊断
4. 可恢复
5. 可交付

## 工作原则

1. 每次只完成用户指定的任务范围。
2. 修改代码前必须说明计划。
3. 禁止自作主张做大规模重构。
4. 禁止删除 Windows 本地启动能力。
5. 禁止修改业务回复逻辑，除非任务明确要求。
6. 禁止把 token、cookie、密钥写入代码。
7. 新增配置必须进入 `.env.example` 或配置文档。
8. 路径必须跨平台，优先使用 `pathlib`、环境变量或配置项。
9. 所有改动必须说明：
   - 修改文件
   - 修改原因
   - 验证方式
   - 潜在风险
   - 回滚方式

## 当前优先级

当前优先级高于一切的是：

1. WebSocket 断线重连稳定性
2. MessageConsumer 并发模型稳定性
3. 任务生命周期和优雅关闭
4. Linux 私有化部署能力
5. 可观测性和诊断能力

## 禁止事项

除非用户明确要求，否则不要：

1. 不要改 FastGPT 业务逻辑。
2. 不要改 MessagePipeline 业务流程。
3. 不要改 AIReplyHandler 的回复策略。
4. 不要改 PyQt UI 交互。
5. 不要改 queue_name，当前一个店铺就是一个账号，暂时继续使用 `pdd_{shop_id}`。
6. 不要引入完整 SaaS、多租户、计费、权限系统。
7. 不要一次性重构多个层级。
